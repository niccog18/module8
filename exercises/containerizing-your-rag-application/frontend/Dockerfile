# frontend/Dockerfile  (STARTER)
# ================================
# Build context: ./frontend
# Runs the Streamlit UI

# TODO: FROM python:3.11-slim
FROM # TODO

WORKDIR /app

# TODO: COPY requirements.txt .
# TODO: RUN pip install --no-cache-dir -r requirements.txt

# TODO: COPY . .

# TODO: EXPOSE 8501

# Streamlit needs --server.address=0.0.0.0 to be reachable from outside the container
# TODO: CMD ["streamlit", "run", "app.py", "--server.port=8501", "--server.address=0.0.0.0"]
CMD # TODO
