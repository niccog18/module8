# backend/Dockerfile  (STARTER)
# ================================
# Build context: ./backend
# Runs the FastAPI RAG API via uvicorn

# TODO: FROM python:3.11-slim
FROM # TODO

WORKDIR /app

# TODO: COPY requirements.txt .
# TODO: RUN pip install --no-cache-dir -r requirements.txt

# TODO: COPY . .   ← copy all backend source files (Dockerfile excludes .env via .dockerignore)

# TODO: EXPOSE 8000

# TODO: CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
CMD # TODO
