# The RAG Architecture — Solution Download

**GitHub:** `module-08-rag-docker/solutions/exercises/rag-architecture/`

Compare your solution to the reference. Key things to check:

- Does your diagram show all 5 stages with the correct technologies?
- Does your prompt builder include a system prompt with "answer from context only" instructions?
- Are retrieved chunks labeled with their source information?
- Does the prompt include citation instructions?
- Is there a token count estimate for prompt length awareness?

Your diagram style and specific prompt wording will differ. The important thing is that all pipeline stages are represented and the prompt follows the RAG pattern.