# frontend/Dockerfile  (STARTER)
# ==================================
# TODO: Complete this Dockerfile for the Streamlit frontend.
# It follows the same pattern as the backend Dockerfile.
#
# Steps to implement:
#   1. Choose a base image — python:3.11-slim
#   2. Set working directory to /app
#   3. Copy requirements.txt and install dependencies
#   4. Copy the rest of the source code
#   5. Expose Streamlit's default port (8501)
#   6. Set the CMD to start Streamlit
#      Hint: streamlit run app.py --server.port=8501 --server.address=0.0.0.0
#
# IMPORTANT: --server.address=0.0.0.0 is required so Streamlit is reachable
# from outside the container (the same reason uvicorn uses --host 0.0.0.0).

# TODO: FROM ...

# TODO: WORKDIR ...

# TODO: COPY requirements.txt .
# TODO: RUN pip install ...

# TODO: COPY . .

# TODO: EXPOSE ...

# TODO: CMD [...]
