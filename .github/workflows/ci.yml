name: RAG Application CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # Job 1: Run Python tests
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./CI_CD/compose-demo/backend  # Changed from just ./backend which run commands from backend dir to ./CI_CD/compose-demo/backend to ensure correct context with my project structure

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest "httpx<0.28"  # Test dependencies

      - name: Run tests
        run: python -m pytest tests/ -v --tb=short
        env:
          OLLAMA_URL: http://localhost:11434  # Won't actually connect in CI
          CHROMA_PATH: ./test_chroma

  # Job 2: Verify Docker builds
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build backend image
        run: docker build -t rag-backend ./CI_CD/compose-demo/backend  # Changed from just ./backend which run commands from backend dir to ./CI_CD/compose-demo/backend to ensure correct context with my project structure

      - name: Build frontend image
        run: docker build -t rag-frontend ./CI_CD/compose-demo/frontend  # Changed from just ./frontend which run commands from frontend dir to ./CI_CD/compose-demo/frontend to ensure correct context with my project structure

      - name: Verify images exist
        run: |
          docker images rag-backend
          docker images rag-frontend

  # Job 3: Lint (code quality)
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Check code formatting
        run: |
          pip install ruff
          ruff check CI_CD/compose-demo/backend/ CI_CD/compose-demo/frontend/
